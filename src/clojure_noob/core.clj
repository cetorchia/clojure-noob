(ns clojure-noob.core
  (:gen-class)
  (:require [clojure-noob.fibonacci])
  (:require [clojure-noob.radial-parts]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "I'm a little teapot (with only green tea in it)!"))
